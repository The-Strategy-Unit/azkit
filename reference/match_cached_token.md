# Find an already cached token that matches desired parameters Sub-routine for [get_auth_token](https://the-strategy-unit.github.io/azkit/reference/get_auth_token.md)

Find an already cached token that matches desired parameters Sub-routine
for
[get_auth_token](https://the-strategy-unit.github.io/azkit/reference/get_auth_token.md)

## Usage

``` r
match_cached_token(resource, tenant, aad_version)
```

## Value

A token from local cache, or NULL if none matches
