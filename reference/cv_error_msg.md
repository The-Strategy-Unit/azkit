# A custom error message generator for the `check_vec` function

A custom error message generator for the `check_vec` function

## Usage

``` r
cv_error_msg(text)
```

## Arguments

- text:

  The main text of the error message
