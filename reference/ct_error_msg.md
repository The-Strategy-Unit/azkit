# A custom error message generator for the `check_that` function

A custom error message generator for the `check_that` function

## Usage

``` r
ct_error_msg(text)
```

## Arguments

- text:

  The main text of the error message
